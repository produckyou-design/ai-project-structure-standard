# ADR-XXXX: (결정 제목)

> 프로젝트에 복사해 쓰는 ADR(Architecture Decision Record) 양식.
> `docs/DECISIONS/ADR-0001-layered-coordinator.md` 와 같은 구조를 따른다.
> 파일명 규칙: `docs/DECISIONS/ADR-<4자리번호>-<짧은-슬러그>.md` (번호는 순차 증가, 재사용하지 않는다).

- 상태: PROPOSED | ACCEPTED | REJECTED | SUPERSEDED (by ADR-XXXX) | DEPRECATED
- 날짜: (YYYY-MM-DD)
- 결정자: (이름 또는 세션/역할)

---

## 1. 맥락

(왜 지금 이 결정이 필요한가. 어떤 문제·제약·요구가 있었는가.
"기존 상태를 조사한 결과"를 먼저 적는다 — 조사 없이 결론부터 쓰지 않는다.)

## 2. 결정

**(한 문장으로 결정 내용을 명시한다.)**

(결정의 세부 내용. 구조도·인터페이스·책임 분리가 있으면 코드블록/표로 명시한다.)

## 3. 고려한 대안

| 대안 | 포기 이유 |
|---|---|
| (대안 1) | |
| (대안 2) | |
| 아무것도 바꾸지 않음 (현상 유지) | (왜 현상 유지가 부족한지) |

## 4. 결과

### 장점

- (이 결정으로 얻는 것)

### 단점

- (이 결정으로 감수하는 것. 없다고 쓰지 않는다 — 모든 결정에는 상충관계가 있다.)

## 5. 위험

- (이 결정이 나중에 문제가 될 수 있는 지점과, 그것을 어떻게 감지할 것인가)
- (예: 책임이 다시 한 곳에 쌓이는 리스크 → `docs/FILE_RESPONSIBILITIES.md` 를 정기적으로 재검토)

## 6. 롤백 방법

- 코드 롤백: (직전 정상 커밋/구조로 되돌리는 구체적 방법)
- 데이터 롤백: (마이그레이션이 관련되면 별도로 명시. 코드 롤백과 데이터 롤백을 분리한다)
- 부분 롤백: (일부 도메인/기능만 이전 상태로 되돌릴 수 있는가)

## 7. 관련 문서

- (이 ADR이 반영되는 표준·템플릿·예제: 예) `docs/ARCHITECTURE_STANDARD.md`, `SKILL.md`)
- (선행/후행 ADR이 있으면 링크)

---

## 작성 규칙

- 계획이 아니라 실제로 검토·결정한 내용을 기록한다 (사후에 다시 쓰지 않는다).
- 포기한 대안과 단점을 숨기지 않는다 — 근거 없는 결정은 신뢰할 수 없다.
- 결정이 뒤집히면 이 문서의 상태를 `SUPERSEDED`로 바꾸고 새 ADR을 만든다.
  기존 ADR 파일을 삭제하거나 그 내용을 지우지 않는다 (기록은 append 우선).
